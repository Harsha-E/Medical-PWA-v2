Plaintext
####################################################################################################
# ================================================================================================ #
#                                                                                                  #
#                     M E D C A R E   V 3   —   A I D E R   S P R I N T                            #
#                                                                                                  #
# ================================================================================================ #
####################################################################################################

====================================================================================================
SECTION 0: AIDER SYSTEM INSTRUCTIONS & CORE PHILOSOPHY
====================================================================================================

ROLE DEFINITION:
You are an expert AI pair programmer operating within the Aider CLI environment. 
You are tasked with executing a massive, multi-phase architectural sprint for MedCare v3.
MedCare v3 is an offline-first Progressive Web Application (PWA).
Your code must be pristine, production-ready, and fully compliant with the rules below.

STRICT EXECUTION RULES FOR AIDER:
1. Do not apologize. Do not explain your thought process. 
2. Output ONLY the necessary SEARCH/REPLACE blocks or full file creations required to complete the tasks.
3. You must strictly adhere to the Vanilla ES6 paradigms. 
4. You are forbidden from introducing any build steps, bundlers, or Node.js dependencies for the UI.
5. All database operations MUST use the existing Dexie.js wrapper.
6. All DOM manipulations MUST use native browser APIs.
7. You must implement exhaustive try/catch blocks for all asynchronous operations.
8. Every new function, method, and class must include a comprehensive JSDoc comment.
9. Follow the exact variable names, console log strings, and logic flows defined in each task below.

====================================================================================================
SECTION 1: GLOBAL ARCHITECTURAL CONSTRAINTS
====================================================================================================

CONSTRAINT 1.1: ZERO-FRAMEWORK POLICY
- You must NOT import, require, or utilize React, Vue, Angular, Svelte, or jQuery.
- The UI layer is 100% Vanilla ES6.
- Component rendering is handled via standard JavaScript classes and template literals.

CONSTRAINT 1.2: DOM MANIPULATION
- Element creation must utilize `document.createElement()`.
- Static HTML injection may use `.innerHTML` ONLY if the string contains no user-generated data.
- Event binding must utilize `.addEventListener()`.
- CSS styling must utilize Tailwind CSS utility classes exclusively.
- Inline styles are forbidden unless dynamically calculating hardware-accelerated transforms.

CONSTRAINT 1.3: DATABASE INTEGRITY (DEXIE.JS)
- Never instantiate `new Dexie()`.
- You must import the singleton database instance: `import db from '../core/db.js';`
- All database queries must be asynchronous (`await`).
- Valid operations: `.add()`, `.put()`, `.where()`, `.modify()`, `.toArray()`.

CONSTRAINT 1.4: "WARM MEDICAL LUXURY" DESIGN LANGUAGE
- The application utilizes a dark mode baseline (`#050203`).
- Overlays must use transparent or semi-transparent RGBA values.
- Buttons and interactive elements must feature glassmorphic properties (`backdrop-blur`).
- Tailwind z-indexes must be carefully managed to avoid overlapping the Ethereal Aura engine.

====================================================================================================
SECTION 2: TASK A — NATIVE PWA INSTALLATION LIFECYCLE
====================================================================================================
TARGET FILE: views/install.js
DEPENDENCIES: window.matchMedia, navigator.standalone

OBJECTIVE:
The `views/install.js` file currently renders a static UI. You must wire this UI to the 
browser's native `beforeinstallprompt` event, allowing the user to trigger the OS-level 
WebAPK installation dialog via a hidden overlay button.

----------------------------------------------------------------------------------------------------
STEP A-1: EVENT CAPTURE AND MODULE STATE
----------------------------------------------------------------------------------------------------
At the very top of `views/install.js` (outside of any class or function), define two variables:
```javascript
/**
 * @type {Event|null}
 * Holds the deferred native install prompt event.
 */
let _deferredPrompt = null;

/**
 * @type {boolean}
 * Flag indicating if the application is currently installable.
 */
let _installable = false;
Next, immediately attach a window event listener for beforeinstallprompt.
Inside the callback:

Call e.preventDefault() to stop the browser's default mini-infobar.

Assign the event e to _deferredPrompt.

Set _installable = true.

Log to console: console.log('[Install View] beforeinstallprompt captured.');

STEP A-2: STANDALONE MODE GUARD
Locate the main render or mount method in views/install.js that injects the DOM.
At the very beginning of this method, implement a strict environmental check.

Logic Flow:
IF window.matchMedia('(display-mode: standalone)').matches IS TRUE
OR IF window.navigator.standalone === true
THEN:

The app is already installed.

Execute: window.location.hash = '#/dashboard';

return; (Halt all further rendering in this view).

STEP A-3: INVISIBLE TRIGGER BUTTON INJECTION
Further down in the render method, after the main glassmorphic CTA card has been created
and appended to the DOM, you must dynamically create a trigger button.

JavaScript
// Pseudo-code expectation:
const triggerBtn = document.createElement('button');
triggerBtn.className = 'absolute inset-0 w-full h-full opacity-0 cursor-pointer z-50';
triggerBtn.setAttribute('aria-label', 'Install MedCare');
Append triggerBtn as a child of the glassmorphic card container.

STEP A-4: BUTTON CLICK HANDLER & NATIVE PROMPT
Attach a 'click' listener to triggerBtn. The callback must be async.
Inside the callback:

Guard Clause: if (!_deferredPrompt) { console.warn('[Install View] No deferred prompt available.'); return; }

Trigger the prompt: _deferredPrompt.prompt();

Await the outcome: const { outcome } = await _deferredPrompt.userChoice;

Log the outcome: console.log(\[Install View] User choice: ${outcome}`);`

Cleanup: Set _deferredPrompt = null; and _installable = false;

STEP A-5: APPINSTALLED CLEANUP
Attach a window event listener for the 'appinstalled' event.
Inside the callback:

Set _deferredPrompt = null;

Set _installable = false;

Log: console.log('[Install View] PWA natively installed.');

Redirect: window.location.hash = '#/dashboard';

====================================================================================================
SECTION 3: TASK B — OCR TO DATABASE PIPELINE
TARGET FILE: views/scan.js
READ-ONLY FILES: core/db.js, core/state.js

OBJECTIVE:
The scanner view currently receives text from Tesseract.js but fails to save it to IndexedDB.
You must intercept the OCR result object, parse it, construct a normalized database record,
save it to the prescriptions table, and conditionally auto-add recognized drugs to the
medications table.

STEP B-1: IMPORTS
Ensure the following exact imports exist at the top of views/scan.js:
import db from '../core/db.js';
import state from '../core/state.js';

STEP B-2: REFACTOR _navigateToAdd()
Locate the existing _navigateToAdd function.
Change its signature to be async and accept exactly one parameter: ocrResult.
JSDoc requirement:

JavaScript
/**
 * Persists the OCR results to the database and navigates to the medications list.
 * @param {Object} ocrResult - The payload from the vision pipeline.
 * @param {string} ocrResult.rawText - The raw string text from Tesseract.
 * @param {string[]} ocrResult.matchedDrugs - Array of drug names identified.
 * @param {Blob|null} ocrResult.capturedImageBlob - The cropped image blob.
 * @returns {Promise<void>}
 */
STEP B-3: PRESCRIPTION RECORD CONSTRUCTION & INSERTION
Inside _navigateToAdd, create a try/catch block.
Inside the try block, construct the record:

JavaScript
const userId = state.get('user').uid;
if (!userId) throw new Error('User not authenticated.');

const prescriptionRecord = {
    userId: userId,
    rawText: ocrResult.rawText || '',
    imageBlob: ocrResult.capturedImageBlob ?? null,
    date: new Date().toISOString().split('T')[0],
    doctorName: null
};
Execute the Dexie add command:
const prescriptionId = await db.prescriptions.add(prescriptionRecord);
console.log('[Scan] Prescription saved. ID:', prescriptionId);

STEP B-4: DRUG CROSS-REFERENCE ALGORITHM
Immediately after saving the prescription, iterate over ocrResult.matchedDrugs:
for (const drugName of ocrResult.matchedDrugs) { ... }

Inside the loop:

Check for existence:
const exists = await db.medications.where('name').equalsIgnoreCase(drugName).first();

If !exists:
Construct a medication record:

JavaScript
const medRecord = {
    userId: userId,
    name: drugName,
    dosage: null,
    frequency: null,
    startDate: new Date().toISOString().split('T')[0],
    endDate: null,
    notes: 'Auto-detected via OCR scan',
    active: true
};
Add it: await db.medications.add(medRecord);
Log it: console.log('[Scan] Auto-added medication from OCR:', drugName);

STEP B-5: ERROR HANDLING AND NAVIGATION
In the catch block of _navigateToAdd:
console.error('[Scan] Failed to save prescription to Dexie:', error);

OUTSIDE and AFTER the try/catch block (so it executes regardless of success/failure):
window.location.hash = '#/medications';

====================================================================================================
SECTION 4: TASK C — DECENTRALIZED MESH & WEBAUTHN
TARGET FILES: services/PeerMesh.js (NEW), views/emergency.js (EDIT)

OBJECTIVE:
Create a WebRTC signaling service using PeerJS. The service must hash the user's Firebase UID
to create a deterministic PeerID. It must intercept all incoming connections and hold them in a
pending state until the user successfully completes a local biometric WebAuthn challenge.

STEP C-1: CREATE PeerMesh.js (SINGLETON PATTERN)
File: services/PeerMesh.js
Imports required:
import Peer from '/vendor/peerjs.min.js';
import state from '../core/state.js';

Implement a strict Singleton class named PeerMesh.
Class properties required:
this._peer = null;
this.myId = null;
this._connections = new Map();
this._pendingConsent = new Map();

STEP C-2: DETERMINISTIC ID GENERATION
Implement an async init() method.

Fetch the UID: const uid = state.get('user').uid;

Hash it using SHA-256:

JavaScript
const encoder = new TextEncoder();
const data = encoder.encode(uid);
const hashBuffer = await crypto.subtle.digest('SHA-256', data);
const hashArray = Array.from(new Uint8Array(hashBuffer));
const hashHex = hashArray.map(b => b.toString(16).padStart(2, '0')).join('');
const deterministicId = hashHex.slice(0, 32);
Instantiate PeerJS: this._peer = new Peer(deterministicId, { debug: 2 });

Bind the 'open' event:
this._peer.on('open', (id) => { this.myId = id; console.log('[PeerMesh] Initialized:', id); });

Bind the 'connection' event to a private _handleIncoming method.

STEP C-3: INCOMING CONNECTION GATING
Implement _handleIncoming(conn):

Store the connection: this._pendingConsent.set(conn.peer, conn);

Do NOT attach data listeners yet.

Dispatch a DOM event to alert the UI:

JavaScript
window.dispatchEvent(new CustomEvent('medcare:peer-request', {
    detail: { 
        peerId: conn.peer, 
        connLabel: conn.metadata?.displayName || 'Unknown Device' 
    }
}));
STEP C-4: WEBAUTHN CHALLENGE
Implement async approvePeerConnection(peerId):

const conn = this._pendingConsent.get(peerId);
if (!conn) throw new Error('No pending connection found for this peer.');

try { ... }

Generate Challenge: const challenge = crypto.getRandomValues(new Uint8Array(32));

Request WebAuthn Assertion:

JavaScript
const assertion = await navigator.credentials.get({
    publicKey: {
        challenge: challenge,
        timeout: 60000,
        userVerification: 'required', // THIS IS MANDATORY
        rpId: window.location.hostname
    }
});
If assertion succeeds:
console.log('[PeerMesh] Biometric consent granted for:', peerId);
this._connections.set(peerId, conn);
this._pendingConsent.delete(peerId);
Call this._openDataChannel(conn);

catch (error) { ... }:
console.error('[PeerMesh] Biometric consent failed or denied.', error);
this._pendingConsent.delete(peerId);
Dispatch medcare:peer-denied event.

STEP C-5: DATA CHANNEL INITIALIZATION
Implement _openDataChannel(conn):

conn.on('open', () => { console.log('[PeerMesh] Data channel open:', conn.peer); });

conn.on('data', (data) => { this._handleData(conn.peer, data); });

conn.on('close', () => { this._connections.delete(conn.peer); });

conn.on('error', (err) => { console.error('[PeerMesh] Conn Error:', err); });

STEP C-6: QR CODE INJECTION IN emergency.js
File: views/emergency.js
Import: import PeerMesh from '../services/PeerMesh.js';
Import: import QRCode from '/vendor/qrcode.min.js';

In the render lifecycle, after DOM is built:

const mesh = PeerMesh.getInstance();

await mesh.init();

Construct URI: const qrUri = `medcare://peer-connect?id=${mesh.myId}`; 

Create canvas: const canvas = document.createElement('canvas');

Render QR:
QRCode.toCanvas(canvas, qrUri, { width: 200, color: { dark: '#ffd9b5', light: '#05020300' } });

Append canvas to the emergency card DOM element.

====================================================================================================
SECTION 5: TASK D — Y.JS CRDT SYNC LAYER
TARGET FILE: services/SyncBridge.js (NEW)

OBJECTIVE:
Create a synchronization bridge that maps a Y.js Conflict-free Replicated Data Type (CRDT) document
to the PeerJS data channel. This ensures that if two connected devices edit the medical profile
simultaneously, the changes merge mathematically without data loss.

STEP D-1: INITIALIZATION
File: services/SyncBridge.js
Imports:
import * as Y from '/vendor/yjs.mjs';
import db from '../core/db.js';
import PeerMesh from './PeerMesh.js';

Implement a Singleton class SyncBridge.
Property: this._docs = new Map(); // Maps peerId to { ydoc, permission }

STEP D-2: ATTACH TO PEER SESSION
Implement async attachToSession(peerId, permissionLevel):

const ydoc = new Y.Doc();

this._docs.set(peerId, { ydoc: ydoc, permission: permissionLevel });

const mesh = PeerMesh.getInstance();

const conn = mesh._connections.get(peerId);

Bind update event:

JavaScript
ydoc.on('update', (update, origin) => {
    if (origin !== 'remote') {
        conn.send({ type: 'yjs-update', payload: Array.from(update) });
    }
});
STEP D-3: OBSERVE AND FLUSH TO DEXIE
In the same method, observe the Y.Map:

const sharedProfile = ydoc.getMap('userProfile');

sharedProfile.observe(async (event) => { ... })

Inside observer:
if (event.transaction.origin === 'remote') {
const updatedData = sharedProfile.toJSON();
await this._flushToDb(peerId, updatedData);
}

STEP D-4: FLUSH METHOD LOGIC
Implement async _flushToDb(peerId, data):

Use a try/catch block.

We must deduce the local user ID to update the correct record.
Assume state.get('user').uid is available globally or imported.

const localUserId = state.get('user').uid;

await db.family.where('userId').equals(localUserId).modify(data);

console.log('[SyncBridge] DB flushed for peer:', peerId);

STEP D-5: WRITE GUARD
Implement setSharedField(peerId, key, value):

JavaScript
const session = this._docs.get(peerId);
if (!session) return;
if (session.permission !== 'READ_WRITE') {
    console.warn('[SyncBridge] Write blocked. Session is READ_ONLY.');
    return;
}
session.ydoc.getMap('userProfile').set(key, value);
====================================================================================================
SECTION 6: TASK E — WEB WORKER VISION PIPELINE
TARGET FILES: workers/vision.worker.js (NEW), services/VisionPipeline.js (EDIT)

OBJECTIVE:
Move all OpenCV, MediaPipe, Tesseract, and NLP regex processing off the main UI thread and into
a dedicated Web Worker to prevent UI freezing during document scanning.

STEP E-1: WORKER BOOTSTRAP (vision.worker.js)
File: workers/vision.worker.js
Must be executed as an ES Module.
Imports required at the top:

JavaScript
import { FilesetResolver, ObjectDetector } from '/vendor/mediapipe/tasks-vision.js';
import '/vendor/opencv.js';
import { createWorker } from '/vendor/tesseract.esm.min.js';
import Fuse from '/vendor/fuse.min.js';
import { drugDataset } from '../data/indian-drug-dataset.js';
Initialize Fuse outside the message handler (so it only builds the index once):

JavaScript
const fuse = new Fuse(drugDataset, {
    keys: ['name', 'brandName'],
    threshold: 0.15,
    distance: 200,
    includeScore: true
});
STEP E-2: MESSAGE INTERCEPTION
self.onmessage = async (e) => { ... }
Check if (e.data.type === 'PROCESS_FRAME')
Extract const bitmap = e.data.bitmap;

STEP E-3: MEDIAPIPE OBJECT DETECTION
Inside the message handler:

const vision = await FilesetResolver.forVisionTasks('/vendor/mediapipe/wasm');

const detector = await ObjectDetector.createFromOptions(vision, { ... });

const detections = detector.detect(bitmap);

Extract the bounding box of detections.detections[0].boundingBox.

Create an OffscreenCanvas matching the bounding box dimensions.

Get the 2D context and drawImage() the cropped area.

Convert canvas to Blob: const croppedBlob = await oc.convertToBlob({ type: 'image/png' });

STEP E-4: OPENCV CLEANING
(Assume croppedBlob is converted back to an ImageBitmap for OpenCV).

const img = cv.imread(imageBitmap);

const gray = new cv.Mat();
cv.cvtColor(img, gray, cv.COLOR_RGBA2GRAY);

const thresh = new cv.Mat();
cv.adaptiveThreshold(gray, thresh, 255, cv.ADAPTIVE_THRESH_GAUSSIAN_C, cv.THRESH_BINARY, 11, 2);

Create an OffscreenCanvas, cv.imshow(canvas, thresh);

const cleanedBlob = await canvas.convertToBlob({ type: 'image/png' });

MANDATORY MEMORY CLEANUP:
img.delete(); gray.delete(); thresh.delete();

STEP E-5: TESSERACT OCR
Initialize Tesseract:

JavaScript
const ocrWorker = await createWorker('eng', 1, {
    workerPath: '/vendor/tesseract/worker.min.js',
    corePath: '/vendor/tesseract/tesseract-core.wasm.js',
    logger: m => self.postMessage({ type: 'OCR_PROGRESS', progress: m.progress })
});
const { data: { text } } = await ocrWorker.recognize(cleanedBlob);

const rawText = text.trim();

Terminate worker: await ocrWorker.terminate();

STEP E-6: NLP REGEX AND FUSE MATCHING
Extract dosages: const dosageRegex = /(?:take\s+)?(\d+(?:\.\d+)?)\s*(mg|mcg|g|ml)/gi;

Extract frequency: const freqRegex = /\b(BID|TID|QID|OD)\b/gi;

Strip matched text to isolate drug candidates.

Split remaining text by commas/newlines into an array candidates.

Iterate candidates and run fuse.search(candidate).

if (results.length > 0 && results[0].score <= 0.15) -> Add to confirmedDrugs array.

STEP E-7: RETURN TO MAIN THREAD
self.postMessage({ type: 'PIPELINE_COMPLETE', result: { rawText, confirmedDrugs, croppedBlob } });

STEP E-8: MAIN THREAD REFACTOR (VisionPipeline.js)
File: services/VisionPipeline.js
In the constructor:
this._worker = new Worker(new URL('../workers/vision.worker.js', import.meta.url), { type: 'module' });

In processFrame(videoElement):

const bitmap = await createImageBitmap(videoElement);

this._worker.postMessage({ type: 'PROCESS_FRAME', bitmap }, [bitmap]); // USE TRANSFERABLE

Return a Promise that listens for PIPELINE_COMPLETE from this._worker.onmessage.

====================================================================================================
SECTION 7: TASK F — DOCLEDGER CRYPTOGRAPHY
TARGET FILES: services/DocLedger.js (NEW), views/history.js (EDIT)

OBJECTIVE:
Compute SHA-256 hashes of all uploaded documents to guarantee clinical integrity. Save the hash
into Dexie, and provide a Fuse.js fuzzy search interface for the history view.

STEP F-1: CREATE DocLedger.js
File: services/DocLedger.js
Imports: import db from '../core/db.js';, import Fuse from '/vendor/fuse.min.js';, import state from '../core/state.js';
Export Singleton class DocLedger.

STEP F-2: HASH AND INGEST
Implement async ingestDocument(file, metadata):

const arrayBuffer = await file.arrayBuffer();

const hashBuffer = await crypto.subtle.digest('SHA-256', arrayBuffer);

Convert hashBuffer to a Hex string (hashHex).

Construct Dexie record for the history table.

Set subresourceIntegrity field to "sha256-" + hashHex.

Set imageBlob field to new Blob([arrayBuffer], { type: file.type }).

const id = await db.history.add(record);

await this._rebuildIndex();

return { docId: id, hashHex };

STEP F-3: INDEX REBUILD & SEARCH
Implement async _rebuildIndex():

this._indexedDocs = await db.history.where('userId').equals(state.get('user').uid).toArray();

this._fuse = new Fuse(this._indexedDocs, { keys: ['title', 'provider'], threshold: 0.3 });

Implement search(query):

if (!query || !this._fuse) return this._indexedDocs;

return this._fuse.search(query).map(r => r.item);

STEP F-4: HISTORY.JS WIRING
File: views/history.js

Import DocLedger.

Programmatically create <input type="file" style="display: none;" accept=".pdf,image/*">.

Append to body.

On file change event: await DocLedger.getInstance().ingestDocument(e.target.files[0], meta);

Locate the existing search bar in the DOM.

Attach input listener to call DocLedger.getInstance().search(e.target.value) and re-render.

====================================================================================================
SECTION 8: TASK G — SERVICE WORKER CACHE MANIFEST
TARGET FILE: sw.js

OBJECTIVE:
You must update the Service Worker precache array to ensure all newly introduced vendor CDN libraries
are aggressively cached for offline use.

STEP G-1: ARRAY MODIFICATION
Locate the CACHE_URLS array at the top of sw.js.
You must ONLY append the following strings exactly as written.
DO NOT modify the fetch, install, or activate event listeners.
DO NOT alter the cache version string.

Append these strings:
'/vendor/peerjs.min.js',
'/vendor/yjs.mjs',
'/vendor/opencv.js',
'/vendor/fuse.min.js',
'/vendor/qrcode.min.js',
'/vendor/mediapipe/tasks-vision.js',
'/vendor/mediapipe/wasm/vision_wasm_internal.js',
'/vendor/mediapipe/wasm/vision_wasm_internal.wasm',
'/vendor/tesseract/worker.min.js',
'/vendor/tesseract/tesseract-core.wasm.js',
'/vendor/tesseract/lang-data/eng.traineddata.gz',
'/ml/document-detector.tflite'

====================================================================================================
SECTION 9: QUALITY ASSURANCE VERIFICATION CHECKLIST
Before finalizing your output, you must internally verify that your proposed code meets these conditions:
[ ] Does views/install.js safely check navigator.standalone without throwing on non-iOS devices?
[ ] Does views/scan.js successfully import the Dexie database singleton instead of instantiating a new one?
[ ] Is the WebAuthn userVerification strictly set to 'required' in PeerMesh.js?
[ ] Are the OpenCV Mat objects explicitly .delete()-ed in the vision worker?
[ ] Does the DocLedger use the native crypto.subtle.digest API for SHA-256 rather than a third-party script?
[ ] Are all async functions wrapped in try/catch blocks with console.error logging?
[ ] Are JSDoc comments applied to every new class and method?

END OF SPRINT SPECIFICATION.
COMMENCE AIDER EXECUTION SEQUENCE.